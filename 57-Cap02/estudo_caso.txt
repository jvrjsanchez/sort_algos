# Estudo de Caso - Usando o ChatGPT Para Construir Um Modelo de Deep Learning


# Prompt 1 (Modelo Simples)

Crie um modelo de Deep Learning. Comece criando uma massa de dados de exemplo com pelo menos 3 variáveis preditoras e uma variável alvo do tipo categórica. Explore rapidamente os dados, faça o pré-processamento, crie uma arquitetura multi-layer perceptron, treine e avalie o modelo usando pelo menos duas métricas. Use Linguagem Python.


# Prompt 2 (Modelo Avançado)

Crie um modelo de Deep Learning. Comece criando dados fictícios que representem uma série temporal de vendas com pelo menos 3 anos de dados (certifique-se de ter algum ruído nos dados representando dados reais). Faça uma rápida exploração dos dados, realize o pré-processamento e então crie um modelo com arquitetura Temporal Fusion Transformer (TFT). Prepare o TimeSeriesDataSet com os dados gerados e aplique GroupNormalizer com transformation="softplus". O modelo TFT deve ser treinado em GPU com learning_rate=0.03 e hidden_size=8. Use loss=QuantileLoss() e optimizer="Ranger". Crie um plot com a performance em treino e use: EarlyStopping(monitor="val_loss", min_delta=1e-4, patience=10, verbose=False, mode="min"). Avalie a performance com a métrica MAE. Use Python e PyTorch. Crie um único bloco de código.


# Prompt 3 ()
